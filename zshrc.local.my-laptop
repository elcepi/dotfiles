alias compi='minicom -b 115200 -o -D /dev/ttyUSB0'
export GPODDER_DOWNLOAD_DIR=${HOME}/Documents/Podcast

HISTSIZE=1000000
alias lsrm='srm -lvr'
ignore_commands=(lsrm ignore-.backup)

plugins=(
  aliases
  common-aliases
  git
  history
  hitchhiker
  )

ZSH_CUSTOM=/home/jose/.dotfiles/custom
